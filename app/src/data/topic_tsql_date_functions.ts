/**
 * Topic.TSQL_DATE_FUNCTIONS — SQL for Data Engineering (T-SQL).
 * Pillar 1 (Querying Foundations): GETDATE, YEAR/MONTH/DAY, DATEPART,
 * DATEDIFF, EOMONTH, FORMAT, CAST(... AS DATE).
 */
import {
  Question,
  QuestionType,
  Difficulty,
  Topic,
  Course,
  CodeLanguage,
} from '../types';

export const tsql_date_functions_questions: Question[] = [
  {
    id: 'tsql-dt-getdate-mcq-1',
    type: QuestionType.MULTIPLE_CHOICE,
    difficulty: Difficulty.BEGINNER,
    topic: Topic.TSQL_DATE_FUNCTIONS,
    course: Course.SQL,
    question: 'What does `GETDATE()` return, and how many times is it evaluated in a single statement?',
    options: [
      { id: 'a', text: "The server's current date and time, evaluated once for the whole statement - every row sees the same value, even across thousands of rows.", isCorrect: true },
      { id: 'b', text: "The client application's current date and time, freshly re-evaluated for every single row it happens to appear on.", isCorrect: false },
      { id: 'c', text: 'The date the table was originally created, a fixed value baked in at creation time that never changes afterward.', isCorrect: false },
      { id: 'd', text: "Only today's calendar date with no time portion at all - GETDATE() never carries an hour, minute, or second.", isCorrect: false },
    ],
    explanation: '`GETDATE()` returns the SQL Server instance\'s current date and time (with a time component). It is evaluated once per statement execution, not once per row - so a multi-row UPDATE/INSERT stamps every affected row with the exact same instant.',
    hints: ['Server time, not client time', 'Evaluated once per statement, not per row'],
    tags: ['tsql', 'date-functions', 'getdate'],
  },
  {
    id: 'tsql-dt-yearmonthday-cloze-1',
    type: QuestionType.CLOZE_CODE,
    difficulty: Difficulty.BEGINNER,
    topic: Topic.TSQL_DATE_FUNCTIONS,
    course: Course.SQL,
    language: CodeLanguage.SQL,
    question: 'Fill in the two functions that extract the year and the month from a date.',
    template: `SELECT OrderId, ___(OrderDate) AS OrderYear, ___(OrderDate) AS OrderMonth
FROM dbo.FactOrders;`,
    blanks: ['YEAR', 'MONTH'],
    solution: `SELECT OrderId, YEAR(OrderDate) AS OrderYear, MONTH(OrderDate) AS OrderMonth
FROM dbo.FactOrders;`,
    explanation: '`YEAR(date)` and `MONTH(date)` extract those parts as plain integers. (A third sibling, `DAY(date)`, extracts the day of month the same way.)',
    hints: ['One function per date part'],
    tags: ['tsql', 'date-functions', 'year', 'month', 'cloze'],
  },
  {
    id: 'tsql-dt-cast-cloze-1',
    type: QuestionType.CLOZE_CODE,
    difficulty: Difficulty.BEGINNER,
    topic: Topic.TSQL_DATE_FUNCTIONS,
    course: Course.SQL,
    language: CodeLanguage.SQL,
    question: "Fill in the function and keyword that strip the time portion off today's date-and-time.",
    template: `SELECT ___(GETDATE() ___ DATE) AS TodayOnly;`,
    blanks: ['CAST', 'AS'],
    solution: `SELECT CAST(GETDATE() AS DATE) AS TodayOnly;`,
    explanation: '`CAST(expression AS type)` converts a value to another data type. `GETDATE()` returns a `DATETIME2` (date + time); casting it `AS DATE` keeps only the calendar date, discarding the time-of-day - the standard way to get "just today\'s date" for comparisons.',
    hints: ['Type-conversion function', 'CAST(expr AS type)'],
    tags: ['tsql', 'date-functions', 'cast', 'cloze'],
  },
  {
    id: 'tsql-dt-datediff-predict-1',
    type: QuestionType.PREDICT_OUTPUT,
    difficulty: Difficulty.BEGINNER,
    topic: Topic.TSQL_DATE_FUNCTIONS,
    course: Course.SQL,
    language: CodeLanguage.SQL,
    question: 'What single value does this query return?',
    code: `SELECT DATEDIFF(DAY, '2026-01-01', '2026-01-10') AS Result;`,
    expectedOutput: `9`,
    explanation: '`DATEDIFF(datepart, start, end)` counts the number of date-part boundaries crossed from start to end - here, 9 whole days separate Jan 1 and Jan 10.',
    hints: ['Counts the boundaries crossed, not the dates themselves'],
    tags: ['tsql', 'date-functions', 'datediff', 'predict'],
  },
  {
    id: 'tsql-dt-eomonth-predict-1',
    type: QuestionType.PREDICT_OUTPUT,
    difficulty: Difficulty.BEGINNER,
    topic: Topic.TSQL_DATE_FUNCTIONS,
    course: Course.SQL,
    language: CodeLanguage.SQL,
    question: 'What single value does this query return?',
    code: `SELECT EOMONTH('2026-02-15') AS Result;`,
    expectedOutput: `2026-02-28`,
    explanation: '`EOMONTH(date)` returns the last calendar day of that date\'s month. 2026 is not a leap year (not divisible by 4), so February ends on the 28th.',
    hints: ['Last day of the given date\'s month', '2026 is not a leap year'],
    tags: ['tsql', 'date-functions', 'eomonth', 'predict'],
  },
  {
    id: 'tsql-dt-datepart-cloze-1',
    type: QuestionType.CLOZE_CODE,
    difficulty: Difficulty.BEGINNER,
    topic: Topic.TSQL_DATE_FUNCTIONS,
    course: Course.SQL,
    language: CodeLanguage.SQL,
    question: 'Fill in the function and the part name that extract which quarter an order falls in.',
    template: `SELECT OrderId, ___(___, OrderDate) AS OrderQuarter
FROM dbo.FactOrders;`,
    blanks: ['DATEPART', 'quarter'],
    solution: `SELECT OrderId, DATEPART(quarter, OrderDate) AS OrderQuarter
FROM dbo.FactOrders;`,
    explanation: '`DATEPART(datepart, date)` is the general-purpose part-extractor - `quarter` is one of many valid parts (others include `year`, `month`, `day`, `weekday`, `hour`). `YEAR()`/`MONTH()`/`DAY()` are just shorthand for the most common cases of `DATEPART`.',
    hints: ['General-purpose part-extraction function', 'Part name for "which quarter"'],
    tags: ['tsql', 'date-functions', 'datepart', 'cloze'],
  },
  {
    id: 'tsql-dt-datediff-age-cloze-1',
    type: QuestionType.CLOZE_CODE,
    difficulty: Difficulty.INTERMEDIATE,
    topic: Topic.TSQL_DATE_FUNCTIONS,
    course: Course.SQL,
    language: CodeLanguage.SQL,
    question: "Fill in the function that computes how many years have passed since SignupDate, using today's date.",
    template: `SELECT CustomerKey, ___(YEAR, SignupDate, GETDATE()) AS YearsSinceSignup
FROM dbo.DimCustomer;`,
    blanks: ['DATEDIFF'],
    solution: `SELECT CustomerKey, DATEDIFF(YEAR, SignupDate, GETDATE()) AS YearsSinceSignup
FROM dbo.DimCustomer;`,
    explanation: '`DATEDIFF(YEAR, start, end)` combined with `GETDATE()` as the end point gives an ever-current "years since" figure that updates automatically every time the query runs, without hard-coding today\'s date.',
    hints: ['Difference-between-dates function', 'End point is the current moment'],
    tags: ['tsql', 'date-functions', 'datediff', 'getdate', 'cloze'],
  },
  {
    id: 'tsql-dt-format-mcq-1',
    type: QuestionType.MULTIPLE_CHOICE,
    difficulty: Difficulty.INTERMEDIATE,
    topic: Topic.TSQL_DATE_FUNCTIONS,
    course: Course.SQL,
    question: "What is the main trade-off of `FORMAT(OrderDate, 'yyyy-MM-dd')` compared to a plain `CAST`/`CONVERT`?",
    options: [
      { id: 'a', text: 'FORMAT supports arbitrary, highly readable custom patterns and locale-aware names but is noticeably slower, since it calls into the .NET formatting engine per value; CAST/CONVERT are faster for standard formats.', isCorrect: true },
      { id: 'b', text: 'FORMAT is always faster than CAST/CONVERT for every case, because it runs natively inside the storage engine rather than as a function call.', isCorrect: false },
      { id: 'c', text: 'FORMAT can only ever be applied to plain integer columns, and it flatly refuses to accept a date or any other date/time-typed value at all.', isCorrect: false },
      { id: 'd', text: 'There is no real difference between the two functions; FORMAT is simply an older, deprecated alias that was kept around purely for CONVERT.', isCorrect: false },
    ],
    explanation: '`FORMAT` is the most flexible way to produce a custom date string (or a localized month/day name), but that flexibility costs performance - it is measurably slower than `CONVERT` with a style code, so it is best reserved for final display formatting rather than large-scale transforms.',
    hints: ['FORMAT = flexible custom patterns, slower', 'CAST/CONVERT = faster, less flexible'],
    tags: ['tsql', 'date-functions', 'format'],
  },
  {
    id: 'tsql-dt-format-cloze-1',
    type: QuestionType.CLOZE_CODE,
    difficulty: Difficulty.INTERMEDIATE,
    topic: Topic.TSQL_DATE_FUNCTIONS,
    course: Course.SQL,
    language: CodeLanguage.SQL,
    question: 'Fill in the function that renders OrderDate as a custom-formatted string.',
    template: `SELECT OrderId, ___(OrderDate, 'yyyy-MM-dd') AS OrderDateText
FROM dbo.FactOrders;`,
    blanks: ['FORMAT'],
    solution: `SELECT OrderId, FORMAT(OrderDate, 'yyyy-MM-dd') AS OrderDateText
FROM dbo.FactOrders;`,
    explanation: '`FORMAT(value, formatString)` renders a date (or number) as text using a custom pattern - `\'yyyy-MM-dd\'` produces a 4-digit year, 2-digit month, 2-digit day.',
    hints: ['Custom-pattern formatting function'],
    tags: ['tsql', 'date-functions', 'format', 'cloze'],
  },
  {
    id: 'tsql-dt-eomonth-datediff-cloze-1',
    type: QuestionType.CLOZE_CODE,
    difficulty: Difficulty.INTERMEDIATE,
    topic: Topic.TSQL_DATE_FUNCTIONS,
    course: Course.SQL,
    language: CodeLanguage.SQL,
    question: "Fill in the function that finds the last day of OrderDate's month, used to compute the days remaining in that month.",
    template: `SELECT OrderId,
       DATEDIFF(DAY, OrderDate, ___(OrderDate)) AS DaysLeftInMonth
FROM dbo.FactOrders;`,
    blanks: ['EOMONTH'],
    solution: `SELECT OrderId,
       DATEDIFF(DAY, OrderDate, EOMONTH(OrderDate)) AS DaysLeftInMonth
FROM dbo.FactOrders;`,
    explanation: '`EOMONTH(OrderDate)` supplies the end-of-month boundary; `DATEDIFF(DAY, OrderDate, ...)` then counts how many days lie between the order and that boundary - a natural pairing of the two functions.',
    hints: ['End-of-month function supplies DATEDIFF\'s end point'],
    tags: ['tsql', 'date-functions', 'eomonth', 'datediff', 'cloze'],
  },
  {
    id: 'tsql-dt-1',
    type: QuestionType.CODING,
    difficulty: Difficulty.INTERMEDIATE,
    topic: Topic.TSQL_DATE_FUNCTIONS,
    course: Course.SQL,
    language: CodeLanguage.SQL,
    question: 'From `dbo.FactOrders` (OrderId, OrderDate), return OrderId and two columns: `OrderYear` (the year of OrderDate) and `OrderMonth` (the month of OrderDate).',
    starterCode: `-- SELECT OrderId, YEAR(OrderDate) AS OrderYear, MONTH(OrderDate) AS OrderMonth FROM dbo.FactOrders;
`,
    testCases: [
      {
        input: 'YEAR(OrderDate), MONTH(OrderDate)',
        expectedOutput: 'OrderId + its order year + its order month',
        description: 'YEAR + MONTH combined',
      },
    ],
    solution: `SELECT OrderId, YEAR(OrderDate) AS OrderYear, MONTH(OrderDate) AS OrderMonth
FROM dbo.FactOrders;`,
    explanation: 'One call to `YEAR` and one to `MONTH`, each aliased, gives the two requested columns directly from OrderDate.',
    hints: ['YEAR(OrderDate) AS OrderYear', 'MONTH(OrderDate) AS OrderMonth'],
    tags: ['tsql', 'date-functions', 'year', 'month'],
  },
  {
    id: 'tsql-dt-2',
    type: QuestionType.CODING,
    difficulty: Difficulty.INTERMEDIATE,
    topic: Topic.TSQL_DATE_FUNCTIONS,
    course: Course.SQL,
    language: CodeLanguage.SQL,
    question: 'From `dbo.DimCustomer` (CustomerKey, SignupDate), return CustomerKey and a column `DaysSinceSignup` giving the number of days between SignupDate and today.',
    starterCode: `-- SELECT CustomerKey, DATEDIFF(DAY, SignupDate, GETDATE()) AS DaysSinceSignup FROM dbo.DimCustomer;
`,
    testCases: [
      {
        input: 'DATEDIFF(DAY, SignupDate, GETDATE())',
        expectedOutput: 'CustomerKey + days elapsed since signup',
        description: 'DATEDIFF + GETDATE combined',
      },
    ],
    solution: `SELECT CustomerKey, DATEDIFF(DAY, SignupDate, GETDATE()) AS DaysSinceSignup
FROM dbo.DimCustomer;`,
    explanation: 'GETDATE() supplies "now" as the end point; DATEDIFF(DAY, ...) counts the days between SignupDate and that moment.',
    hints: ['DATEDIFF(DAY, SignupDate, GETDATE())'],
    tags: ['tsql', 'date-functions', 'datediff', 'getdate'],
  },
  {
    id: 'tsql-dt-3',
    type: QuestionType.CODING,
    difficulty: Difficulty.ADVANCED,
    topic: Topic.TSQL_DATE_FUNCTIONS,
    course: Course.SQL,
    language: CodeLanguage.SQL,
    question: 'From `dbo.FactOrders` (OrderId, OrderDate, Amount), return OrderId and Amount for orders placed more than 30 days before today, comparing only the calendar date and ignoring time-of-day.',
    starterCode: `-- filter FactOrders to orders more than 30 days old, ignoring time-of-day
`,
    testCases: [
      {
        input: "DATEDIFF(DAY, OrderDate, CAST(GETDATE() AS DATE)) > 30",
        expectedOutput: 'Orders older than 30 days',
        description: 'DATEDIFF + CAST + GETDATE combined',
      },
    ],
    solution: `SELECT OrderId, Amount
FROM dbo.FactOrders
WHERE DATEDIFF(DAY, OrderDate, CAST(GETDATE() AS DATE)) > 30;`,
    explanation: '`CAST(GETDATE() AS DATE)` strips today\'s time-of-day so the comparison is purely calendar-based; `DATEDIFF(DAY, OrderDate, ...)` then measures the gap, and the `WHERE` clause keeps only orders where that gap exceeds 30 days.',
    hints: ['CAST(GETDATE() AS DATE) removes the time component', 'DATEDIFF(DAY, OrderDate, that) > 30'],
    tags: ['tsql', 'date-functions', 'datediff', 'cast', 'getdate'],
  },
];
